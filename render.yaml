services:
  - type: web
    name: frontend
    env: docker
    plan: free
    dockerfilePath: ./ej-fe/Dockerfile
    buildFilter:
      paths:
        - ej-fe/**
    region: oregon
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
